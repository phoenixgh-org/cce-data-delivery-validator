# Agent Instructions

**See [`CLAUDE.md`](CLAUDE.md).** It is the single agent-facing doc for this
repo: project overview, the authoritative references, issue-tracking workflow
(this project uses **bd** — run `bd prime`), build and test commands, and the
conventions that apply to code you write here.

This file exists only so agents that look for `AGENTS.md` by name find their way
there. Nothing project-specific lives here — if guidance seems to be missing, it
belongs in `CLAUDE.md`, not in a second copy of it.
